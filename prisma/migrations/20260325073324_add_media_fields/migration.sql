BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Message] ADD [mediaId] NVARCHAR(1000),
[mediaName] NVARCHAR(1000),
[mediaType] NVARCHAR(1000),
[mediaUrl] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
